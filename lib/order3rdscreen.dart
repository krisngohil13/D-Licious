import 'package:flutter/material.dart';

class Order3rdScreen extends StatelessWidget {
  const Order3rdScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        color: Colors.green,
      ),
    );
  }
}
